require stdout

import wyvern.collections.list
import wyvern.Int

type List = list.List	
	
//find the min of list, add it to new list
def min():Unit
    val min = Int.maxValue()
    stdout.printInt(min)